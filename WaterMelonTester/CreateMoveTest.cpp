#include "Tests.hpp"
//#include "WaterMelon.hpp"

void CreateMoveTest()
{
	return;
	//bool Succes = true;
	//Square start = 14;
	//Square target = 4

	//Move move = CreateMove();
	//
	//Assert(GetMoveStart(move) == start,"Start move did not match start move after create and get", &Succes);
	//Assert(GetMoveTarget(move) == target, "Target move did not match target move after create and get", &Succes);

	//if (Succes)
	//	WritePassedTest("CreateMoveTest");
}
#pragma once

#include "Pos.h"

/*
Board is so that the heigher numbers is on the buttom, where white also is.
And black is on the top by the lower numbers.
Left side of board is looking from low numbers down to heigher numbers.
So white is on buttom by default
0 top left
63 buttom right
*/

typedef int Rank;
typedef int Collum;

/*
enum Ranks
{
};

enum Collums
{
};

constexpr Rank GetRank(Pos pos)
{

}

constexpr Collum GetCollum(Pos pos)
{

}
*/